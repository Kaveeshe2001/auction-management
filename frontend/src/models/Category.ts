export type CategoryGet = {
    id: number;
    categoryName: string;
    image: string;
    icon: string;
}

export type CategoryPost = {
    categoryName: string;
    image: string;
    icon: string;
}